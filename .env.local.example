SEEDANCE_API_KEY=

# Optional access protection. Set both in production to require a password.
APP_ACCESS_PASSWORD=
SESSION_SECRET=

# Optional Vercel Blob storage for URL-first reference image uploads.
# Without it, the browser-session Data URL fallback remains available locally.
BLOB_READ_WRITE_TOKEN=
